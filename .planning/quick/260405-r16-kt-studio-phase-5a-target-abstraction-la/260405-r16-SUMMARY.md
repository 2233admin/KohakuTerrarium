---
phase: studio-5a
plan: 01
subsystem: studio/targets
tags: [refactor, abstraction, registry, backward-compatible]
dependency_graph:
  requires: [studio-phase-4-copilot]
  provides: [target-abc, target-registry, claude-code-target, copilot-target, config-target-field, launcher-delegation]
  affects: [config.py, launcher.py]
tech_stack:
  added: []
  patterns: [abc-registry, strategy-delegation]
key_files:
  created:
    - src/kohakuterrarium/studio/targets/__init__.py
    - src/kohakuterrarium/studio/targets/base.py
    - src/kohakuterrarium/studio/targets/claude_code.py
    - src/kohakuterrarium/studio/targets/copilot.py
    - tests/unit/test_studio_targets.py
  modified:
    - src/kohakuterrarium/studio/config.py
    - src/kohakuterrarium/studio/launcher.py
decisions:
  - Base settings loading stays in ProfileLauncher (preserves existing test patch points)
  - ClaudeCodeTarget.merge_settings receives pre-loaded base dict rather than loading internally
  - target field only serialized to YAML when non-default (keeps config files clean)
metrics:
  duration: ~6 min
  completed: 2026-04-05
  lines_added: ~480
  lines_removed: ~74
  tests_added: 16
  tests_total: 141
---

# Phase 5a Plan 01: Target Abstraction + Launcher Refactor Summary

Target ABC + registry pattern decoupling studio from hardcoded Claude Code assumptions, with ClaudeCodeTarget and CopilotTarget concrete implementations.

## Completed Tasks

| # | Task | Commit | Key Changes |
|---|------|--------|-------------|
| 1 | Create targets package | 13e64cd | base.py (Target ABC), __init__.py (registry), claude_code.py, copilot.py |
| 2 | Config target field + launcher delegation | 3dfe8df | ProfileConfig.target, SessionEntry.target, ProfileLauncher delegates |
| 3 | Target-specific tests | 4e9308a | 16 tests covering registry, both targets, integration |

## Architecture

```
ProfileLauncher
  |-- resolve_target(profile.target) --> Target instance
  |-- build_settings_json() --> target.merge_settings(base, profile)
  |-- build_command(path) --> target.build_command(profile, path)

TARGET_REGISTRY
  "claude-code" --> ClaudeCodeTarget
  "copilot"     --> CopilotTarget
```

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Base settings loading location**
- **Found during:** Task 2
- **Issue:** Plan suggested ClaudeCodeTarget.merge_settings load ~/.claude/settings.json internally, but existing tests patch `kohakuterrarium.studio.launcher.CLAUDE_SETTINGS_PATH`. Moving the load into the target would break 3 existing tests.
- **Fix:** Keep base settings loading in ProfileLauncher.build_settings_json(), pass pre-loaded dict to target.merge_settings(base, profile). Target only handles merge logic.
- **Files modified:** launcher.py, targets/claude_code.py
- **Commit:** 3dfe8df

## Verification

- 125 existing tests: PASSED (no modifications)
- 16 new tests: PASSED
- 141 total tests: PASSED
- ruff lint: All checks passed

## Known Stubs

None -- all targets are fully wired with real logic extracted from existing modules.

## Self-Check: PASSED
